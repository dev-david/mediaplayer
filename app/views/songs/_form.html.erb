<%= form_for @song, :html => { :multipart => true } do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div><br>
  <div class="field">
    <%= f.label :artist %><br />
    <%= f.text_field :artist %>
  </div><br>
  <div class="field">
    <%= f.label :mediafile %><br /><br>
    <%= f.file_field :mediafile %>
  </div><br>
  <div class="actions">
    <%= f.button :submit, class: "btn btn-large btn-primary" %>
  </div><br>
<% end %>
